{% extends 'basebackoffice.html.twig' %}
{% block title %}New Organizer{% endblock %}

{% block content %}
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card p-4 shadow-sm">
        <h1 class="mb-4">Create New Organizer</h1>

        {{ include('organizer/_form.html.twig') }}

        <a href="{{ path('app_organizer_index') }}" class="btn btn-secondary mt-3">Back to list</a>
      </div>
    </div>
  </div>
{% endblock %}
